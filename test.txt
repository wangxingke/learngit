为什么每次推送远程仓库时都要填用户和密码。

GitHub上不配置SSH Key 是不是就不能推送文件到远程仓库了，
SSH Key 做下验证。
本地有ssh ，github没有 是可以push到远程仓库的
如果本地版本库与github都没有配饰sshkey 也是可以push到远程仓库的

校验配饰sshkey之后push代码是不是还需要输入用户名和密码

将https协议替换为 ssh协议 

查看是否成功 再次验证是否修改协议成功

删除github的ssh 验证用ssh协议是否可以推送成功

本地多ssh配置

验证 host 属性的作用

测试本地版本库更换本地存储地址，是否还可以连接远程仓库
验证成功，本地版本库可以随便更换存储地址。

验证下